# Copy to .env and fill in
# cp .env.example .env

# Tailscale auth key — create at https://login.tailscale.com/admin/settings/keys
TS_AUTHKEY=

# Your laptop's SSH PUBLIC key (e.g. `cat ~/.ssh/id_ed25519.pub`).
SSH_PUBKEY=

# Where all persistent state lives on the host.
DBOX_DATA=./data
