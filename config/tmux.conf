# Pin panes to fish regardless of how the tmux server was started. The
# entrypoint creates the baseline session via `su dev -s /bin/bash`, so the
# server would otherwise inherit default-shell=/bin/bash and spawn bash panes
# even though dev's login shell is fish.
set -g default-shell /usr/bin/fish

set -g mouse on
set -g history-limit 50000
